# Words from the personal vocabulary that MAY appear on a published page.
#
# The published explainers are on public URLs. The personal word list is, by
# construction, the words English does not know about whoever uses this — names
# of people, places, courses, shorthand. So the rule inverts the usual burden:
# a vocabulary word found in an artifact is treated as a leak unless it is
# listed here, and adding a word here is a deliberate act.
#
# What belongs here: general terms anyone would recognise, and terms about the
# Jewish calendar, which is a capability of the system and in scope for the
# pages. What never belongs here: a name, a place, an institution, a course, or
# a piece of personal shorthand — however harmless it looks on its own.
#
# One word per line, lowercase. Blank lines and # comments ignored.

# Jewish calendar — the pages describe this as a feature, so the terms are needed
shabbat
yom kippur
kippur
shiur
fast day

# Ordinary English that happens to be in the word list
tomorrow
# 'night' is in the word list as a correction target, but on these pages it is
# plain English doing necessary work: "in a single night", and the Hebrew
# calendar's day boundary, where "a Saturday night" is the precise phrase.
# Rewording either would cost clarity to dodge a word that says nothing personal.
night

# Invented examples used throughout the pages
dentist appointment
set a meeting

# Tooling named in passing
claude
gemini

# A keyword argument in the Hebrew-calendar library's public API, shown in a
# code sample: festival(israel=True) selects the one-day festival schedule.
# A country name, not a fact about anyone.
israel

# NOTE ON FALSE POSITIVES
# The word list holds ordinary English words as well as personal ones, so this
# check occasionally flags an innocent one. Prefer rewording the sentence — it
# is usually clearer anyway — over adding the word here. Everything above
# should be a word that genuinely had to appear.
# generic terms for Jewish prayer and Torah reading — the observance gate's
# own category names on the engine explainer, not personal details
davening
leyning

# General English terms that also happen to be in the word list. Declared one
# by one, with the reason, because the case-sensitive short-word check
# (test_artifact_claims, 2026-09-24) now reads 3-4 letter entries too.
api
gpu
llm
# "mark" — an English verb ("mark it done"), and the internals page's point is
# precisely a word that is both a common name and a completion verb in the
# rule table; the brief (ARTIFACT_BUILDER.md) prescribes it as the invented
# example. Never used on a page for anything the author did.
mark
# "google" — the company; the pages name the calendar providers a user can
# connect (Google, Outlook), which is a capability, not anything personal.
google
