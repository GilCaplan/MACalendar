"""The v2 judge set — commands built from a grammar, damaged by observed ops.

`README.md` is the reference; `generate_v2.py` builds the two files and
`verify_v2.py` refuses the set when an invariant is broken.
"""
